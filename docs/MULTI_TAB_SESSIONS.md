# Поддержка независимых сессий в разных вкладках

## Проблема

Изначально приложение использовало `localStorage` для хранения токенов и данных пользователя. Это приводило к следующим проблемам:

### ❌ До исправления:
1. **Shared Storage**: `localStorage` является общим для всех вкладок одного домена
2. **Перезапись токенов**: При авторизации во второй вкладке токен первого пользователя перезаписывался
3. **Нет изоляции**: Невозможно было работать с разными пользователями в разных вкладках
4. **Конфликты сессий**: WebSocket соединения конфликтовали между вкладками

### Пример проблемы:
```
Вкладка 1: Авторизован пользователь A (token_A в localStorage)
Вкладка 2: Авторизуется пользователь B (token_B перезаписывает token_A)
Вкладка 1: Теперь использует token_B (пользователь A "выбит" из системы)
```

---

## Решение: sessionStorage

Было принято решение использовать `sessionStorage` вместо `localStorage`:

### ✅ После исправления:
1. **Изоляция вкладок**: `sessionStorage` уникален для каждой вкладки
2. **Независимые сессии**: Каждая вкладка имеет свою собственную сессию
3. **Множественные пользователи**: Можно работать с разными пользователями одновременно
4. **Независимые WebSocket**: Каждая вкладка имеет свое WebSocket соединение

### Пример работы:
```
Вкладка 1: Авторизован пользователь A (token_A в sessionStorage вкладки 1)
Вкладка 2: Авторизован пользователь B (token_B в sessionStorage вкладки 2)
Обе вкладки работают независимо! ✅
```

---

## Внесенные изменения

### 1. frontend/src/stores/auth.ts

Заменены все обращения к `localStorage` на `sessionStorage`:

```typescript
// ❌ Было:
const savedToken = localStorage.getItem('token')
localStorage.setItem('token', response.token)
localStorage.removeItem('token')

// ✅ Стало:
const savedToken = sessionStorage.getItem('token')
sessionStorage.setItem('token', response.token)
sessionStorage.removeItem('token')
```

**Затронутые функции:**
- `init()` - инициализация при загрузке
- `verifyCode()` - сохранение токена после авторизации
- `logout()` - удаление токена при выходе

### 2. frontend/src/services/api.ts

Обновлены interceptors для работы с `sessionStorage`:

```typescript
// ❌ Было:
const token = localStorage.getItem('token')
localStorage.removeItem('token')

// ✅ Стало:
const token = sessionStorage.getItem('token')
sessionStorage.removeItem('token')
```

**Затронутые части:**
- Request interceptor - добавление токена к запросам
- Response interceptor - обработка 401 ошибок

---

## Тестирование

### Сценарий тестирования:

1. ✅ **Авторизация первого пользователя**
   - Вкладка 1: Авторизован как "Первый Пользователь" (79111111111)
   - Токен сохранен в sessionStorage вкладки 1

2. ✅ **Авторизация второго пользователя**
   - Вкладка 2: Авторизован как "Второй Пользователь" (79222222222)
   - Токен сохранен в sessionStorage вкладки 2

3. ✅ **Проверка изоляции**
   - Вкладка 1: `sessionStorage.getItem('user')` → Первый Пользователь
   - Вкладка 2: `sessionStorage.getItem('user')` → Второй Пользователь

4. ✅ **Выход из второй вкладки**
   - Вкладка 2: Выполнен logout
   - Вкладка 1: Первый пользователь остался авторизованным!

### Результаты:
```bash
✅ Вкладка 1: "User: Первый Пользователь (79111111111)"
✅ Вкладка 2: "User: Второй Пользователь (79222222222)"
✅ После выхода из вкладки 2, вкладка 1 осталась авторизованной
```

---

## Преимущества

### 1. Гибкость для разработки
- Можно тестировать разных пользователей одновременно
- Не нужно выходить/входить при переключении пользователей
- Удобно для демонстрации и отладки

### 2. Безопасность
- Токены автоматически удаляются при закрытии вкладки
- Меньше риск утечки токенов (не сохраняются между сессиями браузера)
- Изоляция между вкладками предотвращает случайные конфликты

### 3. Пользовательский опыт
- Можно работать с несколькими аккаунтами
- Поддержка use-case для службы поддержки (работа с несколькими клиентами)
- Каждая вкладка - независимая сессия

---

## Недостатки и компромиссы

### ⚠️ Потеря сессии при закрытии вкладки
- **Было**: Токен сохранялся между сессиями браузера (localStorage)
- **Стало**: Токен удаляется при закрытии вкладки (sessionStorage)

**Решение**: Пользователь должен авторизоваться заново при открытии новой вкладки.

### ⚠️ Не сохраняется между перезагрузками
- **Было**: Можно закрыть браузер и вернуться - оставаться авторизованным
- **Стало**: При перезагрузке браузера нужна повторная авторизация

**Решение**: Если нужна персистентность, можно добавить опцию "Remember me" с localStorage.

---

## Альтернативные решения (не реализованы)

### 1. Гибридный подход
```typescript
// Использовать localStorage для "Remember me"
const storage = rememberMe ? localStorage : sessionStorage;
storage.setItem('token', token);
```

**Плюсы**: Пользователь выбирает сам  
**Минусы**: Усложнение логики

### 2. IndexedDB
```typescript
// Использовать IndexedDB с вкладко-специфичными ключами
await db.sessions.put({ tabId, token, user });
```

**Плюсы**: Больше контроля, больше места  
**Минусы**: Больше сложности, асинхронность

### 3. BroadcastChannel API
```typescript
// Синхронизация между вкладками через BroadcastChannel
const channel = new BroadcastChannel('auth');
channel.postMessage({ type: 'logout', userId });
```

**Плюсы**: Возможность координации между вкладками  
**Минусы**: Усложнение, не решает основную проблему

---

## Рекомендации

### Для разработки:
✅ Используйте разные вкладки для тестирования разных пользователей  
✅ Проверяйте WebSocket события в каждой вкладке независимо  
✅ Помните, что закрытие вкладки сбросит сессию

### Для production:
⚠️ Рассмотрите добавление опции "Remember me" для удобства пользователей  
⚠️ Информируйте пользователей о том, что сессия привязана к вкладке  
⚠️ Рассмотрите увеличение времени жизни JWT токена (сейчас 48 часов)

---

## Заключение

Переход на `sessionStorage` решил проблему конфликтующих сессий в разных вкладках и позволил работать с несколькими пользователями одновременно. Это особенно полезно для:

- 🧪 **Тестирования** - быстрое переключение между пользователями
- 👥 **Поддержки** - работа с несколькими клиентами
- 🔐 **Безопасности** - автоматическое удаление токенов при закрытии вкладки
- 🎯 **Разработки** - удобная отладка с разными учетными записями

**Дата внедрения**: 05.11.2025  
**Версия**: 1.1.0  
**Статус**: ✅ Протестировано и работает

