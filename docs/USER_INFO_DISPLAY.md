# Отображение информации о текущем пользователе

**Дата**: 05.11.2025  
**Версия**: 1.1.1  
**Статус**: ✅ Реализовано и улучшено

---

## Проблема

До внесения изменений:
- ❌ На странице чата не было видно, какой пользователь авторизован
- ❌ При работе с несколькими вкладками было сложно понять, под каким пользователем работаешь
- ❌ Отсутствовал визуальный индикатор текущей сессии

### После первой реализации:
- ⚠️ Карточка между "Чаты" и списком диалогов создавала впечатление, что это диалог с пользователем
- ⚠️ Неправильная иерархия интерфейса

## Решение

**Финальная версия**: Информационная карточка пользователя размещена **над** заголовком "Чаты" как header/идентификатор текущей сессии.

### Структура интерфейса:
```
┌─────────────────────────────────────┐
│ [Карточка пользователя]             │ ← Header сессии
├─────────────────────────────────────┤
│ Чаты                                │ ← Заголовок раздела
├─────────────────────────────────────┤
│ [Список диалогов]                   │ ← Контент
└─────────────────────────────────────┘
```

### Компоненты карточки:

1. **Аватар с инициалами**
   - Круглый аватар с градиентом (синий → индиго)
   - Отображаются инициалы пользователя (первые буквы имени и фамилии)
   - Размер: 40x40 пикселей (компактный)
   - Тень для глубины

2. **Имя пользователя**
   - Полное имя из профиля
   - Полужирный шрифт, небольшой размер (text-sm)
   - Обрезается, если слишком длинное (truncate)

3. **Телефон**
   - Форматированный номер телефона: +7 XXX XXX-XX-XX
   - Серый цвет, маленький размер шрифта (text-xs)

4. **Кнопка выхода**
   - Иконка выхода справа в карточке
   - Серый цвет с hover эффектом
   - Компактная (w-5 h-5)

5. **Фон**
   - Градиент от голубого к индиго (from-blue-50 to-indigo-50)
   - Визуально выделяет header от остального контента

---

## Реализация

### Измененные файлы

**`frontend/src/views/ChatView.vue`**

#### Добавлен HTML блок (размещен над заголовком "Чаты"):
```vue
<!-- Current User Info -->
<div v-if="authStore.user" class="p-3 bg-gradient-to-r from-blue-50 to-indigo-50 border-b border-gray-200">
  <div class="flex items-center space-x-3">
    <!-- Avatar with initials -->
    <div class="w-10 h-10 rounded-full bg-gradient-to-br from-blue-500 to-indigo-600 flex items-center justify-center text-white font-semibold shadow-md">
      {{ getUserInitials(authStore.user.name) }}
    </div>
    
    <!-- User info -->
    <div class="flex-1 min-w-0">
      <div class="font-semibold text-gray-900 text-sm truncate">
        {{ authStore.user.name }}
      </div>
      <div class="text-xs text-gray-600">
        {{ formatPhone(authStore.user.phone) }}
      </div>
    </div>

    <!-- Logout button -->
    <button
      @click="logout"
      class="flex-shrink-0 text-gray-500 hover:text-gray-900 transition-colors"
      title="Выйти"
    >
      <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 16l4-4m0 0l-4-4m4 4H7m6 4v1a3 3 0 01-3 3H6a3 3 0 01-3-3V7a3 3 0 013-3h4a3 3 0 013 3v1" />
      </svg>
    </button>
  </div>
</div>

<!-- Header -->
<div class="p-4 border-b border-gray-200">
  <h1 class="text-xl font-bold text-gray-900">Чаты</h1>
</div>
```

#### Добавлены функции:

**1. Получение инициалов:**
```typescript
function getUserInitials(name: string): string {
  if (!name) return '?'
  
  const parts = name.trim().split(' ')
  if (parts.length >= 2) {
    // Берем первую букву имени и фамилии
    return (parts[0][0] + parts[1][0]).toUpperCase()
  }
  // Если только одно слово, берем первые 2 буквы
  return name.substring(0, 2).toUpperCase()
}
```

**Примеры:**
- "Иван Петров" → "ИП"
- "Anna Smith" → "AS"
- "Пользователь" → "ПО"
- "" → "?"

**2. Форматирование телефона:**
```typescript
function formatPhone(phone: string): string {
  if (!phone) return ''
  
  // Format: 79123456789 -> +7 912 345-67-89
  if (phone.startsWith('7') && phone.length === 11) {
    return `+7 ${phone.substring(1, 4)} ${phone.substring(4, 7)}-${phone.substring(7, 9)}-${phone.substring(9)}`
  }
  
  return phone
}
```

**Примеры:**
- "79123456789" → "+7 912 345-67-89"
- "79111111111" → "+7 911 111-11-11"
- "invalid" → "invalid"

---

## Визуальный дизайн

### Цветовая схема:

- **Фон карточки**: Градиент `from-blue-50 to-indigo-50`
- **Аватар**: Градиент `from-blue-500 to-indigo-600`
- **Текст имени**: `text-gray-900` (темно-серый)
- **Текст телефона**: `text-gray-600` (серый)
- **Индикатор онлайн**: `bg-green-500` (зеленый) с анимацией pulse
- **Граница**: `border-gray-200`

### Отступы и размеры:

- Padding карточки: `p-3` (12px) - компактный header
- Отступы между элементами: `space-x-3` (12px)
- Размер аватара: `w-10 h-10` (40x40px) - компактный
- Размер кнопки выхода: `w-5 h-5` (20x20px)
- Размер текста имени: `text-sm` (14px)
- Размер текста телефона: `text-xs` (12px)

---

## Примеры использования

### Пример 1: Первый пользователь
```
┌─────────────────────────────────────┐
│ ┌──┐                                │
│ │ПП│ Первый Пользователь     [⎋]   │ ← Header сессии
│ └──┘ +7 911 111-11-11              │
├─────────────────────────────────────┤
│ Чаты                                │ ← Заголовок раздела
├─────────────────────────────────────┤
│ [Список диалогов]                   │
└─────────────────────────────────────┘
```

### Пример 2: Второй пользователь
```
┌─────────────────────────────────────┐
│ ┌──┐                                │
│ │АИ│ Анна Иванова             [⎋]   │ ← Header сессии
│ └──┘ +7 922 222-22-22              │
├─────────────────────────────────────┤
│ Чаты                                │ ← Заголовок раздела
├─────────────────────────────────────┤
│ [Список диалогов]                   │
└─────────────────────────────────────┘
```

---

## Тестирование

### Тест-кейсы:

#### Тест 1: Отображение инициалов
- **Входные данные**: "Первый Пользователь"
- **Ожидаемый результат**: "ПП"
- **Результат**: ✅ УСПЕХ

#### Тест 2: Форматирование телефона
- **Входные данные**: "79111111111"
- **Ожидаемый результат**: "+7 911 111-11-11"
- **Результат**: ✅ УСПЕХ

#### Тест 3: Множественные пользователи
- **Вкладка 1**: "Первый Пользователь (ПП)"
- **Вкладка 2**: "Анна Иванова (АИ)"
- **Результат**: ✅ УСПЕХ - каждая вкладка показывает своего пользователя

#### Тест 4: Анимация онлайн-индикатора
- **Проверка**: Зеленая точка пульсирует
- **Результат**: ✅ УСПЕХ - анимация работает

---

## Преимущества

### Для пользователей:
- 👤 **Идентификация** - сразу видно, кто авторизован
- 🔍 **Прозрачность** - понятно, под каким аккаунтом работаешь
- 🎨 **Визуализация** - красивый аватар с инициалами

### Для разработки:
- 🧪 **Тестирование** - легко определить, под каким пользователем работаешь
- 🐛 **Отладка** - быстрая проверка сессии
- 👥 **Множественные вкладки** - каждая вкладка показывает своего пользователя

### Для UX:
- 📱 **Контекст** - пользователь всегда знает, где он находится
- 🎯 **Ориентация** - карточка сверху, всегда на виду
- 💚 **Статус** - индикатор онлайн показывает активность

---

## Адаптивность

### Desktop (текущая реализация):
- Ширина sidebar: 320px (w-80)
- Все элементы умещаются в одну строку
- Полный текст имени и телефона

### Mobile (будущее улучшение):
- Можно скрыть телефон на маленьких экранах
- Аватар можно сделать меньше
- Статус "онлайн" можно показывать только точкой

---

## Возможные улучшения

### Краткосрочные:
1. **Редактирование профиля** - кнопка для изменения имени
2. **Загрузка аватара** - возможность загрузить фото
3. **Статусы** - выбор статуса (онлайн, отошел, занят)
4. **Настройки** - выпадающее меню при клике на карточку

### Долгосрочные:
1. **Присутствие** - реальный онлайн/оффлайн статус
2. **Последняя активность** - "был(а) 5 минут назад"
3. **Аватар из Gravatar** - автоматическая загрузка по email
4. **Цветовые темы** - выбор цвета аватара

---

## Технические детали

### Зависимости:
- Vue 3 Composition API
- Tailwind CSS для стилей
- Pinia store (authStore) для данных пользователя

### Производительность:
- Функции `getUserInitials` и `formatPhone` вызываются только при рендере
- Нет лишних вычислений в runtime
- Условный рендеринг `v-if="authStore.user"`

### Безопасность:
- Данные берутся из authStore (проверенный источник)
- Нет утечки чувствительной информации
- Телефон форматируется на клиенте (не влияет на безопасность)

---

## Скриншоты

1. `user-info-card.png` - Первый пользователь
2. `user-info-card-user2.png` - Второй пользователь
3. `user-info-card-comparison.png` - Сравнение двух пользователей

---

## Заключение

Добавление карточки с информацией о пользователе значительно улучшило UX приложения, особенно при работе с множественными сессиями в разных вкладках. Теперь пользователь всегда знает, под каким аккаунтом он работает.

**Статус**: ✅ Завершено и протестировано  
**Готовность к production**: ✅ Да  
**Требуется код-ревью**: ⏳ Желательно

